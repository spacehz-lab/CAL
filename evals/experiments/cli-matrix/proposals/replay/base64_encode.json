{
  "metadata": {
    "source": "cli-matrix",
    "prompt_version": "matrix-v1",
    "model": "fixture",
    "schema_version": "proposal.v1"
  },
  "verifier_packages": [
    {
      "id": "base64_matches_source",
      "description": "Passes when target is the Base64 encoding of source.",
      "verify_py": "import json\nimport os\nimport sys\n\nrequest = json.load(sys.stdin)\nverifier_id = request[\"verifier\"][\"id\"]\ninputs = request.get(\"inputs\") or {}\n\ndef fail(code, message):\n    print(json.dumps({\"passed\": False, \"error\": {\"code\": code, \"message\": message}}))\n    sys.exit(0)\n\ndef passed(content):\n    print(json.dumps({\n        \"passed\": True,\n        \"evidence\": [{\"id\": verifier_id, \"type\": verifier_id, \"content\": content}],\n        \"outputs\": content,\n    }))\n    sys.exit(0)\n\nimport base64\nsource = inputs.get(\"source\")\ntarget = inputs.get(\"target\")\nwith open(source, \"rb\") as handle:\n    expected = base64.b64encode(handle.read()).decode(\"ascii\")\nwith open(target, \"r\", encoding=\"utf-8\") as handle:\n    actual = \"\".join(handle.read().split())\nif actual != expected:\n    fail(\"base64_mismatch\", \"target is not the Base64 encoding of source\")\npassed({\"source\": source, \"target\": target})\n"
    }
  ],
  "candidates": [
    {
      "capability_id": "text.encode_base64",
      "description": "Encode text content into Base64 text.",
      "execution": {
        "kind": "cli",
        "spec": {
          "args": [
            "-i",
            "{{source}}"
          ],
          "stdout_path_input": "target"
        }
      },
      "rationale": "CLI matrix replay proposal"
    }
  ],
  "probe_plans": [
    {
      "candidate_index": 0,
      "inputs": {
        "target": "{{workdir}}/encoded.txt"
      },
      "fixtures": [
        {
          "input": "source",
          "filename": "input.txt",
          "content": "hello"
        }
      ],
      "verifier": {
        "id": "base64_matches_source"
      }
    }
  ]
}
