{
  "metadata": {
    "source": "cli-matrix",
    "prompt_version": "matrix-v1",
    "model": "fixture",
    "schema_version": "proposal.v1"
  },
  "verifier_packages": [
    {
      "id": "text_equals_cal",
      "description": "Passes when the target text equals cal.",
      "verify_py": "import json\nimport os\nimport sys\n\nrequest = json.load(sys.stdin)\nverifier_id = request[\"verifier\"][\"id\"]\ninputs = request.get(\"inputs\") or {}\n\ndef fail(code, message):\n    print(json.dumps({\"passed\": False, \"error\": {\"code\": code, \"message\": message}}))\n    sys.exit(0)\n\ndef passed(content):\n    print(json.dumps({\n        \"passed\": True,\n        \"evidence\": [{\"id\": verifier_id, \"type\": verifier_id, \"content\": content}],\n        \"outputs\": content,\n    }))\n    sys.exit(0)\n\ntarget = inputs.get(\"target\")\nwith open(target, \"r\", encoding=\"utf-8\", errors=\"ignore\") as handle:\n    content = handle.read().strip()\nif content != \"cal\":\n    fail(\"text_mismatch\", \"target text does not equal cal\")\npassed({\"target\": target})\n"
    }
  ],
  "candidates": [
    {
      "capability_id": "text.transcode",
      "description": "Transcode text content between character encodings.",
      "execution": {
        "kind": "cli",
        "spec": {
          "args": [
            "-f",
            "UTF-8",
            "-t",
            "UTF-8",
            "{{source}}"
          ],
          "stdout_path_input": "target"
        }
      },
      "rationale": "CLI matrix replay proposal"
    }
  ],
  "probe_plans": [
    {
      "candidate_index": 0,
      "inputs": {
        "target": "{{workdir}}/transcoded.txt"
      },
      "fixtures": [
        {
          "input": "source",
          "filename": "input.txt",
          "content": "cal"
        }
      ],
      "verifier": {
        "id": "text_equals_cal"
      }
    }
  ]
}
