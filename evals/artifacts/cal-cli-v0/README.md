# CAL CLI v0 Release Artifacts

This directory is reserved for sanitized release artifacts that correspond to
reported evaluation tables.

Do not copy raw `CAL_HOME`, API keys, raw prompts, full LLM responses, or
machine-specific dumps here. Keep raw run outputs under `evals/out/`.
